
{
    "scripts": {
        "frontend:install": "cd udagram-frontend && npm install",
        "api:install": "cd udagram-api && npm install",
        "frontend:build": "cd udagram-frontend && npm run e2e && npm run build ",
        "api:build": "cd udagram-api && npm run build"
    }
,
    "devDependencies": {}
}