import dotenv from "dotenv";
dotenv.config();
const {
   POSTGRES_PORT,
   POSTGRES_HOST,
   POSTGRES_DB,

   POSTGRES_USER,
   POSTGRES_PASSWORD,AWS_ACCESS_KEY_ID,
   PEPPER,
   SALT,
   TOKEN_SECRET,AWS_SECRET_ACCESS_KEY,
} = process.env;
export default {
     POSTGRES_HOST,
   POSTGRES_PORT,
      POSTGRES_DB,
    POSTGRES_USER,
     POSTGRES_PASSWORD,
     PEPPER,
     SALT,
     TOKEN_SECRET,AWS_ACCESS_KEY_ID,AWS_DEFAULT_REGION,AWS_SECRET_ACCESS_KEY
 };